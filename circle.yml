## Customize the test machine
machine:

## Customize dependencies
dependencies:
  pre:
    - npm install -g gulp
    - npm install -g bower
    - npm install
    - bower install

## Customize test commands
test:
  override:
    - gulp serve
