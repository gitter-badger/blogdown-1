## Customize machine
machine:
  node:
    version: 4.4.0

## Customize dependencies
dependencies:
  override:
    - npm install -g gulp
    - npm install -g bower
    - npm install
    - bower install

## Customize test commands
test:
  override:
    - gulp
